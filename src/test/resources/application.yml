spring:
  flyway:
    enabled: false
  redis:
    host: localhost
    port: 6379
  datasource:
    url: jdbc:postgresql://localhost:5432/crm_dev?currentSchema=crm
    username: postgres
    password: root